### ### ### Czy podany kod jest poprawny składniowo w Pythonie? Jeśli nie, to dlaczego?

### x = 2; y = 3;
### if (x > y):
###     result = x;
### else:
###     result = y;
# Program uruchomi się, jednak nie jest on do końca poprawny składniowo, ponieważ w Pythonie nie używa się średników na końcu.

### for i in "axby": if ord(i) < 100: print (i)
# Nie jest poprawny składniowo, ponieważ nie jest tu zastosowane odpowienie formatowanie kodu. Nie ma wcięcia, przez co kod się nie wykona.

### for i in "axby": print (ord(i) if ord(i) < 100 else i)
# Tak, jest poprawny składniowo.